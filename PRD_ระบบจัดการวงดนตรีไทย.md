# Product Requirements Document (PRD)
## ระบบจัดการวงดนตรีไทย (Thai Music Ensemble Management System)

**เวอร์ชันเอกสาร:** 4.1
**วันที่อัปเดต:** 16 กันยายน 2569
**เวอร์ชันก่อนหน้า:** 4.0 (16 กันยายน 2569) · 3.0 (22 กรกฎาคม 2569)

**สรุปการเปลี่ยนแปลง v4.1:** ขยายหัวข้อ 3.8 (สมุดโน้ตเพลง) ให้ครอบคลุมความต้องการที่เพิ่มเข้ามา — copy/cut, เส้นขอบ, จัดตำแหน่งแนวตั้ง, wrap text, ย้าย/แทรกแถว-คอลัมน์, fill handle — พร้อมกำกับสถานะจริงของแต่ละข้อจากการตรวจโค้ด และระบุขอบเขตที่ไม่ทำ (ระบบสูตรคำนวณ) ไว้ชัดเจน

---

## 1. ภาพรวมโปรเจกต์ (Overview)

ระบบเว็บแอปพลิเคชันสำหรับบริหารจัดการวงดนตรีไทย (ชมรม/ชุมนุมดนตรีไทยในสถาบันการศึกษา) ครอบคลุมการจัดการสมาชิก เพลงและโน้ตเพลง การจัดงานแสดง การจัดผังเวที ตารางซ้อม และคลังอุปกรณ์ เพื่อทดแทนกระบวนการที่เดิมทำผ่าน Excel และการสื่อสารแบบกระจัดกระจาย ให้รวมศูนย์ข้อมูลและลดความผิดพลาด เช่น การจองเครื่องดนตรีซ้ำซ้อนในเพลงเดียวกัน

### เป้าหมายหลัก
- ลดความซ้ำซ้อนและความผิดพลาดในการจัดสรรเครื่องดนตรีต่อเพลง
- ให้สมาชิกเห็นภาพรวมงานแสดง วันซ้อม และสถานะของตนเองได้ในที่เดียว
- ให้ Admin ควบคุมและตรวจสอบย้อนหลังการเปลี่ยนแปลงผังเวทีและตารางซ้อมได้
- แปลงข้อมูลโน้ตเพลงจาก Excel เดิมเข้าสู่ระบบดิจิทัลโดยไม่เปลี่ยนรูปแบบที่ผู้ใช้คุ้นเคย

### เทคโนโลยีที่ใช้ (Tech Stack)

| ชั้น | เทคโนโลยี |
|---|---|
| Framework | Next.js 16 (App Router) + React 19 |
| ภาษา | TypeScript |
| ฐานข้อมูล | PostgreSQL 15+ ผ่าน Prisma 7 ORM |
| Authentication | NextAuth 5 (Google OAuth Provider + Credentials เฉพาะบัญชีชั่วคราว, bcryptjs) |
| Styling | Tailwind CSS 4 |
| Excel I/O | `xlsx` |
| Word Export | `docxtemplater` + `pizzip` |
| Image capture | `html-to-image` (สำหรับ export ผังเวที) |
| Email | Resend (สำหรับแจ้งเตือน admin/ผลการอนุมัติ) |

---

## 2. กลุ่มผู้ใช้งาน (User Roles)

| Role | คำอธิบาย |
|---|---|
| สมาชิก (MEMBER) | ผู้เล่นดนตรีทั่วไป ดูเพลง, ตอบ RSVP, จองคิวเครื่องดนตรี, ดูตารางซ้อม |
| หัวหน้าเครื่อง (HEAD) | สิทธิ์เหมือนสมาชิก + แก้ไขผังเวที/จัดคิวได้เฉพาะงานแสดงที่ตนถูกกำหนดเป็นหัวหน้า |
| Admin (ADMIN) | สิทธิ์เต็มทุกส่วน รวมจัดการสมาชิก ตารางซ้อม audit log |

สถานะบัญชี: `ACTIVE` / `EXPIRED` — รองรับ **บัญชีชั่วคราว (Temporary Account)** สำหรับผู้ร่วมแสดงเฉพาะกิจ พร้อม utility สำหรับล้างบัญชีที่หมดอายุ

---

## 3. ขอบเขตฟังก์ชัน (Functional Requirements)

### 3.1 ระบบสมาชิก (Authentication & Profile)

> **เปลี่ยนแปลงสำคัญใน v4.0:** ระบบ login/register ของ**สมาชิกทุกคน** เปลี่ยนจาก email+password มาใช้ **"Sign in with Google"** — NextAuth Credentials เดิมคงไว้เฉพาะ**บัญชีชั่วคราว** (FR-1.5) เท่านั้น ดูเหตุผลและสถานะการ implement ในหัวข้อ 5

- FR-1.1: **Self-registration ผ่าน Google** — สมาชิกกดปุ่ม **"Sign in with Google"** จากหน้า Login ไม่มีฟอร์มกรอกอีเมล/รหัสผ่านอีกต่อไป (ไม่มีทั้ง self-registration แบบเดิมและ Supabase email-link verification)
- FR-1.1.1: **Verified email ผ่าน Google OAuth** — Google เป็นผู้ยืนยันตัวตนและอีเมลให้แล้วในขั้นตอน OAuth consent จึงไม่ต้องมีขั้นตอนส่ง verification email แยกต่างหากอีก (ตัด Supabase Auth ออกจากระบบทั้งหมด)
- FR-1.1.2: **Admin approval (deny-by-default ยังคงอยู่)** — ครั้งแรกที่ผู้ใช้ sign in ด้วย Google สำเร็จ → สร้าง row `User` ใน Prisma ผูกกับ Google account (เช่น ด้วย email หรือ Google `sub`) สถานะ `PENDING_APPROVAL` และ **ยังเข้าระบบไม่ได้** จนกว่า admin จะอนุมัติผ่านหน้า admin panel (อนุมัติ / ปฏิเสธ / ระบุเหตุผล) — พฤติกรรม deny-by-default นี้เหมือนเดิมทุกประการ เปลี่ยนแค่วิธียืนยันตัวตนเท่านั้น
- FR-1.1.3: **Notification** — เมื่อมีบัญชีใหม่รอการอนุมัติ ระบบส่งแจ้งเตือน (email + in-app notice) ไปยัง admin ทุกคน และเมื่อ admin อนุมัติ/ปฏิเสธ ระบบแจ้งกลับให้ผู้สมัครทางอีเมล (ใช้ Resend เหมือนเดิม — ส่วนนี้ไม่กระทบจากการเปลี่ยน auth)
- FR-1.1.4: **Session** — Login ใช้ NextAuth Google OAuth Provider เป็นช่องทางหลัก session/JWT callback เพิ่มเงื่อนไข `status === "ACTIVE"` เหมือนเดิม ก่อนอนุญาตให้เข้าระบบ

**สถาปัตยกรรม auth (v4.0 — Google-first + Credentials เฉพาะบัญชีชั่วคราว):**
- **NextAuth Google Provider**: ช่องทางเดียวของ**สมาชิกทุกคน** รับผิดชอบทั้งการยืนยันตัวตนและอีเมล (แทน Supabase Auth + Credentials เดิม)
- **NextAuth Credentials Provider**: คงไว้ **เฉพาะบัญชีชั่วคราว** (`isTemporary = true`) เท่านั้น — ดู FR-1.5
- **Prisma `User`**: ยังเป็น source of truth ของข้อมูลสมาชิก (nickname, role, status, …) — สมาชิกปกติผูกกับ Google account ด้วยฟิลด์ผูกบัญชี (เช่น `googleId`/email) แทน `supabaseUserId` เดิม; `passwordHash` ยังคงอยู่ใน schema แต่ใช้กับบัญชีชั่วคราวเท่านั้น (สมาชิกปกติเป็น null)
- **Migration สมาชิกเดิม**: สมาชิกที่เคย login ด้วย email+password ต้อง sign in ด้วย Google account ที่ใช้อีเมลตรงกับบัญชีเดิมเพื่อ "link" เข้ากับ row `User` เดิมโดยอัตโนมัติ (จับคู่ด้วย email) ไม่ต้องสมัครใหม่และไม่ต้องผ่าน admin approval ซ้ำถ้าบัญชีเดิม `status = ACTIVE` อยู่แล้ว — หลัง migration ต้อง**ล้าง `passwordHash` ของสมาชิกปกติทิ้ง**เพื่อปิดทางเข้าเดิม
- FR-1.2: โปรไฟล์สมาชิก: ชื่อเล่น, รุ่น, เครื่องดนตรีหลัก, เครื่องดนตรีรอง, รูปโปรไฟล์ (avatar upload — เริ่มต้นดึงจากรูป Google ได้ แต่ยังแก้ไขเองได้)
- FR-1.3: ประวัติการแสดงย้อนหลังของสมาชิก — บันทึกอัตโนมัติจากระบบจองคิวเครื่องดนตรี
- FR-1.5: **บัญชีชั่วคราว (Temporary Account) — login ด้วย Credentials** สำหรับผู้ร่วมแสดงที่ไม่ได้เป็นสมาชิกถาวรและอาจไม่มี/ไม่สะดวกผูก Google account:
  - สร้างโดย **admin เท่านั้น** (ไม่ผ่าน self-registration) โดย admin ตั้ง username/อีเมล + รหัสผ่านให้แล้วส่งให้ผู้ร่วมแสดงโดยตรง — บัญชีถูก mark `isTemporary = true` และผูกกับงานแสดง (`linkedPerformanceId`)
  - บัญชีชั่วคราวที่ admin สร้าง **ข้ามขั้น `PENDING_APPROVAL`** ได้ทันที (`status = ACTIVE`) เพราะ admin เป็นผู้สร้างเอง = อนุมัติในตัว
  - **ข้อจำกัดสำคัญ (deny-by-default):** `authorize()` ของ Credentials provider ต้องปฏิเสธทุก user ที่ `isTemporary !== true` เสมอ เพื่อกันไม่ให้สมาชิกปกติย้อนกลับไป login ด้วย password ได้ และต้องเช็ค `status === "ACTIVE"` เหมือน provider หลัก
  - ไม่มี self-service password reset สำหรับบัญชีชั่วคราว — ให้ admin ตั้งรหัสใหม่ให้แทน (ดู FR-5.4)
  - หมดอายุแล้วเปลี่ยนสถานะเป็น `EXPIRED` พร้อม utility ล้างบัญชีที่หมดอายุ (เหมือนเดิม)

~~FR-1.4: Password Reset ผ่านอีเมล~~ — **ตัดออก** ไม่มี password ในระบบอีกต่อไปหลังเปลี่ยนเป็น Google-only login

**สถานะบัญชี (Account Status):**
| สถานะ | ความหมาย | เข้าระบบได้? |
|---|---|---|
| `PENDING_APPROVAL` | Sign in with Google สำเร็จครั้งแรก รอ admin อนุมัติ | ❌ |
| `ACTIVE` | อนุมัติแล้ว | ✅ |
| `REJECTED` | admin ปฏิเสธ | ❌ |
| `SUSPENDED` | ถูกระงับภายหลัง | ❌ |
| `EXPIRED` | บัญชีชั่วคราวหมดอายุ (ดู FR-1.5) | ❌ |
| ~~`PENDING_EMAIL`~~ | เลิกใช้ — ไม่มีขั้นตอนยืนยันอีเมลของระบบเองแล้ว (ยังคงค่าไว้ใน enum `AccountStatus` เพื่อไม่ให้ migration พัง) | ❌ |

---

### 3.2 หน้า Dashboard

- FR-2.1: สรุปงานแสดงที่กำลังจะถึง
- FR-2.2: Calendar interactive แสดง performance date และ practice day รวมกัน (พร้อม localization ปี พ.ศ.)

---

### 3.3 หน้าเพลง (Song Library)

- FR-3.1: ค้นหาเพลงแบบ non-case sensitive (ค้นตามชื่อหรือรหัสเพลง)
- FR-3.2: กรองเพลงตามประเภท (genre/category)
- FR-3.3: ข้อมูลต่อเพลง: ชื่อเพลง, ประเภท, โน้ตเพลง, เวลาที่ใช้เล่น, **BPM เริ่มต้น (defaultBpm)**, **time signature เริ่มต้น (defaultTimeSig)**
- FR-3.4: Micro-adjust — Admin แก้ไขโน้ตเพลงทีละช่องได้โดยตรงจากหน้าเว็บ (`PATCH /api/songs/[id]/sheet`)
- FR-3.5: Song Version History — บันทึกทุกการแก้ไขโน้ตเพลง พร้อม restore เวอร์ชันก่อนหน้าได้

#### 3.3.1 ระบบ Import เพลง

- FR-3.6: Import จาก Excel (`.xlsx`) แบบ bulk ผ่าน drag-and-drop พร้อม preview และแจ้งเตือนแถวผิดรูปแบบ
- FR-3.7: กรอกโน้ตเพลงด้วยมือเป็นกริด: 1 บรรทัด = 8 ห้อง, 1 ห้อง = 4 ช่อง
- FR-3.8: แต่ละช่องรับ text อิสระ รองรับอักขระและสัญลักษณ์ไทย (ด ร ม ฟ ซ, ตัวเลข, `-`, สัญลักษณ์หายใจ)

---

### 3.4 หน้าการแสดง (Performance)

#### 3.4.1 การจัดการงานแสดง

- FR-4.1: ปฏิทินกิจกรรมแสดงงานแสดงทั้งหมด
- FR-4.2: สร้างงานแสดงใหม่ เลือกได้หลายวันต่องาน (PerformanceDate) พร้อมระบุนักแสดงที่เข้าร่วม
- FR-4.3: ค้นหาและเพิ่มเพลงเข้างานแสดง (non-case sensitive, กรองตามประเภท)
- FR-4.4: แสดงรายการเพลงทั้งหมดที่ถูกเพิ่มเข้างาน
- FR-4.5: RSVP — สมาชิกยืนยันสถานะว่าง/ไม่ว่าง **แยกเป็นรายวัน (per PerformanceDate)**
- FR-4.6: Admin เพิ่ม/ลบสมาชิกเข้า/ออกรายการงานแสดงโดยตรง (PerformanceMember)
- FR-4.7: Performance Head Assignment — กำหนดหัวหน้าแยกต่อ performance (PerformanceHead)
- FR-4.8: **Export รายงานงานแสดงเป็นไฟล์ Word (.docx)** พร้อมฝังภาพผังเวที (html-to-image)

#### 3.4.2 การจัดลำดับเพลง (Running Order)

- FR-4.9: กำหนดลำดับเพลงในงานแสดง (PerformanceSong.order)
- FR-4.9.1: **แบ่งเพลงเป็นช่วง (PerformanceSection)** — Admin/HEAD สร้างช่วงในงานแสดง (เช่น "ไหว้ครู", "ออกจากห้องประชุม") แล้วจัดเพลงเข้าแต่ละช่วง
  - **ช่วงว่างได้** (มีช่วงโดยไม่มีเพลง)
  - **1 เพลง อยู่ได้ 1 ช่วงเท่านั้น** (`sectionId` เป็น FK เดียว)
  - **ไม่มีเวลาต่อช่วง** (ไม่เก็บ startTime/endTime ระดับช่วง)
  - เพลงที่ไม่ได้จัดช่วง (`sectionId = null`) แสดงในกลุ่มพิเศษ **"ยังไม่จัดช่วง"** ที่ด้านบนสุดของ running order
  - Drag-and-drop สองระดับ: ย้ายเพลงข้ามช่วง + ย้ายลำดับช่วงทั้งหมด
  - ลบช่วง → เพลงในช่วงกลายเป็น "ยังไม่จัดช่วง" (ไม่ลบเพลง)
- FR-4.10: เลือกสมาชิกที่เล่นในแต่ละเพลงและเลือกเครื่องดนตรีให้แต่ละคน (SongAssignment)
- FR-4.10.1: **User sheet preference ต่อ performance** — ในหน้า `/performances/[id]/sheets` ผู้ใช้แต่ละคนเลือก "ค่าเริ่มต้น" ของประเภทโน้ต (เช่น "เครื่องนำ", "เครื่องตาม", "เครื่องสาย") ที่จะแสดงในทุกเพลง พร้อม override เฉพาะเพลงได้:
  - **Toolbar sticky ด้านบนสุด** ของหน้า พร้อม dropdown ประเภท + ปุ่ม "บันทึก"
  - เลือก default → เพลงทั้งหมด switch tab ทันที; เพลงที่ไม่มี sheet ตามชื่อที่เลือก **fallback** ไป sheet แรก + badge เตือน (เช่น "ใช้เครื่องนำแทน — ไม่มีเครื่องตาม")
  - Override เฉพาะเพลง: คลิก tab ในเพลงใดเพลงหนึ่ง → override เฉพาะเพลงนั้น
  - **บันทึกแบบ explicit** เท่านั้น (auto-save ไม่มี); dirty state โชว์บนปุ่ม + `beforeunload` เตือนถ้ายังไม่บันทึก
  - **Persist per-user, per-performance** ใน `UserPerformanceSheetPref` + `UserSongSheetOverride`
  - **หน้าเพลงเดี่ยว (`/songs/[id]`) ไม่ใช้ preference นี้** — คงพฤติกรรมเดิม (default "เครื่องนำ" หรือ sheet แรก)

#### 3.4.3 ระบบจองคิวเครื่องดนตรีต่อเพลง

- FR-4.11: สมาชิกเลือกเครื่องดนตรีที่จะเล่นในแต่ละเพลงของงานแสดง
- FR-4.12: Validation ป้องกันการจองซ้ำซ้อน (ยกเว้นเครื่องที่กำหนดให้เล่นพร้อมกันได้หลายคน) — *สถานะ: ยังไม่ implement, ดูหัวข้อ 5*

#### 3.4.4 ผังเวที (Stage Layout)

- FR-4.13: สร้างผังเวทีแบบ dynamic กำหนดขนาดเองได้ (widthUnits × heightUnits) พร้อมกำหนดหน่วย (unitLabel)
- FR-4.14: ลากเครื่องดนตรีวางบนผังเวที ขนาดไอคอนปรับสัดส่วนตามขนาดเวที
- FR-4.15: กำหนดชื่อเครื่องดนตรีและ footprint แบบ custom ได้
- FR-4.16: Equipment setup ต่อ stage item (ผูกอุปกรณ์เข้ากับตำแหน่งบนเวที)
- FR-4.17: กำหนดสมาชิกไปยัง stage item แต่ละตำแหน่งได้
- FR-4.18: Version History ทุกครั้งที่แก้ไขผัง (StageLayoutVersion) พร้อมย้อนดูและกู้คืนเวอร์ชันก่อนหน้า
- FR-4.19: บันทึกผังเวทีเป็น Template (StageTemplate) เพื่อนำกลับมาใช้ซ้ำกับงานแสดงอื่น

---

### 3.5 สิทธิ์ Admin

- FR-5.1: Admin ดูข้อมูลสมาชิกทั้งหมด (ชื่อเล่น, รุ่น, เครื่องดนตรีหลัก-รอง) พร้อม filter/search
- FR-5.2: สร้างตารางซ้อม เลือกงานแสดงที่จะซ้อมและกำหนดวันที่ซ้อม
- FR-5.3: Role-based Access Control — HEAD แก้ไขได้เฉพาะงานที่ตนถูก assign, ADMIN แก้ได้ทุกส่วน
- FR-5.4: **Password Reset โดย Admin — เฉพาะบัญชีชั่วคราว** (FR-1.5) เท่านั้น; สมาชิกปกติไม่มี password ให้ reset แล้ว (login ผ่าน Google) admin จัดการได้ด้วยการอนุมัติ/ปฏิเสธ/ระงับบัญชี (`ACTIVE` / `REJECTED` / `SUSPENDED`) แทน
- FR-5.5: Audit Log บันทึกผู้แก้ไข วันเวลา เชื่อมกับ Version History (append-only) — *สถานะ: schema พร้อม แต่ยังไม่ wire เข้า endpoint, ดูหัวข้อ 5*

---

### 3.6 ตารางซ้อม (Practice Schedule)

**Flow ใหม่ (2 เฟส):** ก่อนสร้าง PracticeSchedule จริง ต้องเปิด **Availability Poll** ให้สมาชิกกดว่าว่างช่วงไหนก่อน admin จึงเลือกวัน/เวลาจากผลโพลได้

#### เฟส 1 — Availability Poll (ก่อนสร้างตารางซ้อม)

- FR-6.0.1: **สร้างโพล** — Admin/HEAD เปิดโพลผูกกับงานแสดง ระบุ candidate time slots (วันที่ + ช่วงเวลา หลายช่อง) และ deadline สิ้นสุดการโหวต
- FR-6.0.2: **สมาชิกกดว่าง** — สมาชิกในงานแสดงเปิดโพลได้ ติ๊กช่องเวลาที่ตัวเองว่าง (per-slot boolean); update ทันทีแบบ optimistic
- FR-6.0.3: **Heatmap view** — Admin เห็นผลโพลเป็นแถบสี ความเข้มของสีสัมพันธ์กับ `count(available=true)` ต่อ slot (เขียวเข้ม = คนว่างเยอะ, จาง = น้อย); แสดงจำนวนคนว่าง / คนทั้งหมดกำกับทุกช่อง
- FR-6.0.4: **ปิดโพล + เลือกวัน** — Admin กด "สร้างตารางซ้อม" จากหน้าโพล → เห็นเฉพาะช่วงที่มีคนติ๊กว่างอย่างน้อย 1 คน → เลือก slot ที่ต้องการ (multi-select) → ระบบสร้าง PracticeSchedule + PracticeDay + PracticeSlot จากสิ่งที่เลือก โดยผูก candidate userIds ของแต่ละ slot ให้เป็นค่าเริ่มต้นของ PracticeAvailability
- FR-6.0.5: **Notification** — เมื่อ Admin เปิดโพล ระบบส่งแจ้ง (email + in-app) ให้สมาชิกในงานแสดง; ก่อน deadline 24 ชม. เตือนคนที่ยังไม่ตอบ

**Data model — Availability Poll:**

```
AvailabilityPoll
├── id
├── performanceId  (FK — ผูกกับงานแสดง)
├── name
├── deadline?      (optional; หมดเขตกดว่าง)
├── timezone       (default "Asia/Bangkok")
├── status         (OPEN | CLOSED)
├── createdById
└── createdAt

AvailabilityPollSlot
├── id
├── pollId         (FK)
├── date           (YYYY-MM-DD)
├── startTime      (HH:MM)
├── endTime        (HH:MM)
└── slotOrder

AvailabilityPollResponse
├── id
├── pollSlotId     (FK)
├── userId         (FK — สมาชิกในงานแสดง)
├── isAvailable    (bool)
├── updatedAt
└── @@unique([pollSlotId, userId])
```

**Heatmap query:**
```sql
SELECT ps.id, ps.date, ps.startTime, COUNT(r.userId) FILTER (WHERE r.isAvailable) AS available
FROM AvailabilityPollSlot ps
LEFT JOIN AvailabilityPollResponse r ON r.pollSlotId = ps.id
WHERE ps.pollId = $1
GROUP BY ps.id;
```

#### เฟส 2 — สร้างตารางซ้อม (มีอยู่แล้ว + ปรับ)

- FR-6.1: ตารางซ้อมแบบละเอียด แบ่งเป็น PracticeSchedule → PracticeDay → PracticeSlot → PracticeMemberGroup รองรับกลุ่มย่อยภายในการซ้อม
- FR-6.1.1: **บังคับสร้างจาก poll** — ห้ามสร้าง PracticeSchedule โดยไม่เคยเปิด poll ก่อน (ยกเว้น admin กด "ข้ามโพล" พร้อมเหตุผล) — โยง `PracticeSchedule.sourcePollId` อ้างอิงกลับไปที่โพลต้นทาง
- FR-6.2: Availability Grid — สมาชิกระบุความพร้อมรายช่วงเวลาในแต่ละวันซ้อม (PracticeAvailability)
- FR-6.3: Collapsible sections — ซ่อน/แสดงกลุ่มซ้อมได้
- FR-6.4: รองรับหลาย schedule ต่อ 1 งานแสดง
- FR-6.5: Summary endpoint สำหรับตรวจสอบการตอบรับ (`/api/practice-schedules/[id]/summary`)

---

### 3.7 รายการอุปกรณ์ (Equipment Inventory)

- FR-7.1: ตารางคลังอุปกรณ์: ID, รายการ, จำนวน, จำนวนที่ชำรุด (brokenQuantity), ชนิด, ขนาด (ยาว/กว้าง/สูง ซม.), หมายเหตุ
- FR-7.2: Global Instrument Equipment — ตารางอุปกรณ์ที่ผูกกับเครื่องดนตรีแต่ละชนิดในระดับ global (ชื่อ / จำนวนเก้าอี้ / จำนวนโต๊ะ ต่อเครื่องดนตรี)

> **หมายเหตุ:** ระบบยืม-ให้ยืมอุปกรณ์ (Borrow/Lend, ยืม vs ให้ยืม) เคยพัฒนาไว้แต่ถูก **revert ออกจาก main แล้ว** (commit 61d1d5b) — ปัจจุบันไม่มี field เกี่ยวกับ loan ใน Equipment model

---

### 3.8 สมุดโน้ตเพลง (Sheet Music Editor)

ระบบแก้ไขโน้ตเพลงในรูปแบบตาราง หน้าตาและการใช้งานคล้าย Excel แต่ **ไม่มีการคำนวณสูตร** — ใช้สำหรับจดและแสดงโน้ตเพลงไทยเป็นหลัก

#### โครงสร้างตาราง

- FR-8.1: ตารางโน้ตมีค่าเริ่มต้น 8 คอลัมน์ต่อแถว (ปรับเปลี่ยนได้)
- FR-8.2: รองรับการพิมพ์ภาษาไทย ภาษาอังกฤษ และอักขระพิเศษ เช่น สระ/วรรณยุกต์ซ้อน (ฺ ํ ็ ่ ้ ๊ ๋)

> **สัญลักษณ์สถานะ:** ✅ ใช้งานได้แล้ว · ⚠️ มีแล้วแต่ยังทำงานไม่ถูกต้อง · ❌ ยังไม่มี
> (สถานะ ณ 16 ก.ย. 2569 จากการตรวจโค้ด `components/sheets/SheetGrid.tsx` และ `Toolbar.tsx`)

#### 3.8.1 จัดการเซลล์

- FR-8.3: ✅ Merge Cell — รวมเซลล์ได้ทั้งแนวนอนและแนวตั้ง (MergedCell)
- FR-8.3.1: ✅ Unmerge Cell — แยกเซลล์ที่รวมไว้
- FR-8.5: ✅ ปรับขนาดคอลัมน์และแถวได้อิสระ (ColumnWidth / RowHeight)
- FR-8.10: ✅ **เพิ่ม/ลบแถวและคอลัมน์** — ลบแล้วข้อมูลถัดไปเลื่อนขึ้น/ชิดซ้ายจริง พร้อมขยับ `MergedCell`, `ColumnWidth`, `RowHeight` ให้ตรงกัน · แทรกตรงตำแหน่งที่เลือกได้ (ใต้/ขวาของเซลล์ที่เลือก) หรือต่อท้ายถ้าไม่ได้เลือก · ย้อนด้วย undo ได้
  - ขยับที่ฝั่ง DB ผ่าน `POST /api/sheets/[id]/structure` ใน transaction เดียว เพื่อให้ `CellStyle` ที่ผูกกับ `cellId` ตามไปเอง
  - merge ที่คร่อมแถว/คอลัมน์ที่ถูกลบจะหดลง 1 ช่อง ถ้าอยู่บนแถวนั้นช่องเดียวจะถูกลบทิ้ง
  - **ข้อจำกัดที่ทราบ:** การลบทำให้ข้อมูลบางอย่างหายถาวร (ขอบเขต merge ที่หดไป, ความสูงแถวที่ถูกลบ) undo จึงกู้จากสถานะที่ server คืนกลับมาเทียบกับ snapshot ไม่ใช่กลับด้านคำสั่งอย่างเดียว
- FR-8.12: ❌ **ย้ายแถว / คอลัมน์** — สลับตำแหน่งได้ (ใช้กลไกขยับพิกัดชุดเดียวกับ FR-8.10)
- FR-8.13: ❌ **Drag & Drop ข้อมูล** — ลากเซลล์หรือกลุ่มเซลล์ไปวางตำแหน่งอื่น
- FR-8.14: ✅ **Copy / Cut / Paste** — ใช้รูปแบบ TSV (คั่นด้วย Tab) เดียวกันทั้งขาเข้าและขาออก จึง copy/paste กับ Excel ได้สองทาง; การเลือกแบบไม่เป็นสี่เหลี่ยม (Ctrl+คลิก) จะส่งออกเป็นกรอบสี่เหลี่ยมที่ครอบทั้งหมด
- FR-8.15: ✅ **Undo / Redo** — Ctrl+Z / Ctrl+Y และปุ่ม ↶ ↷ บน toolbar · ครอบ **ค่าในเซลล์, การจัดรูปแบบ, merge/unmerge, ปรับขนาดแถว-คอลัมน์, เพิ่ม/ลบแถว-คอลัมน์** และบันทึกการย้อนลง DB ทุกชนิด (การปรับขนาดนับเป็น 1 ครั้งต่อการลาก ไม่ใช่ต่อการขยับเมาส์)
- FR-8.16: ✅ เลือกหลายเซลล์พร้อมกัน (ลากเมาส์ / Shift / Ctrl)

#### 3.8.2 จัดการข้อความ

- FR-8.9: ✅ ตัวหนา (Bold) / ตัวเอียง (Italic) / ขีดเส้นใต้ (Underline)
- FR-8.6: ✅ ปรับขนาดตัวอักษรต่อเซลล์ (รองรับหลายขนาดในเซลล์เดียวผ่าน rich text)
- FR-8.7: ✅ เปลี่ยนฟอนต์ได้ (ค่าเริ่มต้น: Sarabun รองรับภาษาไทย)
- FR-8.8: ✅ จัดตำแหน่งแนวนอน — ซ้าย / กลาง / ขวา
- FR-8.4: ✅ สีพื้นหลังเซลล์ (Highlight)
- FR-8.17: ✅ สีตัวอักษร
- FR-8.18: ✅ **จัดตำแหน่งแนวตั้ง** — บน / กลาง / ล่าง
- FR-8.19: ✅ **Wrap Text** — ตัดคำขึ้นบรรทัดใหม่ในเซลล์ (มีผลทั้งตอนแสดงผลและตอนกำลังพิมพ์)
- FR-8.20: ❌ **ปุ่มเพิ่ม/ลดขนาดตัวอักษร** (A+ / A−) — ปัจจุบันมีแต่ช่องกรอกตัวเลข
- FR-8.21: ❌ **ลบ Format** — ล้างการจัดรูปแบบของเซลล์ที่เลือกกลับเป็นค่าเริ่มต้น

#### 3.8.3 เส้นตาราง / รูปแบบเซลล์

- FR-8.22: ✅ **Border** — กำหนดเส้นขอบได้ทีละด้าน (บน / ล่าง / ซ้าย / ขวา) และรอบทั้งหมด
- FR-8.23: ✅ **ปรับความหนาและรูปแบบเส้นขอบ** (เช่น ทึบ / ประ / คู่)
- FR-8.24: ❌ **ซ่อน/แสดงเส้นตาราง (Gridline)** — ระดับทั้งชีต

> **หมายเหตุ:** FR-8.18, FR-8.19 และ FR-8.22–8.23 ทำผ่าน migration `20260916120000_cell_border_align_wrap` ซึ่งเพิ่ม `verticalAlign`, `wrapText` และ `borderTop/Right/Bottom/Left` เข้า `CellStyle` · เส้นขอบเก็บเป็น CSS shorthand ด้านละคอลัมน์ (เช่น `"2px dashed #000000"`) ค่าว่าง = ไม่มีเส้น · ส่วน FR-8.24 (gridline) ยังไม่ได้ทำ เพราะเป็นค่าระดับชีตไม่ใช่ระดับเซลล์

#### 3.8.4 การเลือกและลาก (ให้ใช้งานใกล้เคียง Excel)

| พฤติกรรม | สถานะ |
|---|---|
| คลิกเซลล์ → เลือกเซลล์ | ✅ |
| ลากเมาส์ → เลือกหลายเซลล์ | ✅ |
| ลากหัวคอลัมน์ → เปลี่ยนความกว้าง | ✅ |
| ลากหัวแถว → เปลี่ยนความสูง | ✅ |
| ลากขอบ selection → ย้ายข้อมูล | ❌ (FR-8.13) |
| ลากมุมเซลล์ → Fill / Copy ข้อมูล | ❌ (FR-8.25) |
| ลากหัวคอลัมน์ → เปลี่ยนตำแหน่งคอลัมน์ | ❌ (FR-8.12) |
| ลากหัวแถว → เปลี่ยนตำแหน่งแถว | ❌ (FR-8.12) |

#### 3.8.5 สิ่งที่ **ไม่ทำ** (out of scope โดยเจตนา)

ระบบนี้ใช้จดและแสดงโน้ตเพลงไทย **ไม่ใช่เครื่องมือคำนวณ** จึงตัดสิ่งเหล่านี้ออกทั้งหมด:

❌ สูตรคำนวณทุกชนิด (`=SUM()`, `=AVERAGE()`, `=IF()`) · ❌ Formula bar · ❌ Function library · ❌ Auto calculation · ❌ Pivot Table · ❌ Conditional calculation

> การตัดสินใจนี้มีผลต่อการเลือกเครื่องมือด้วย — ไลบรารีตารางสำเร็จรูปส่วนใหญ่ผูกมากับระบบสูตร จึงแบกน้ำหนักที่ไม่ได้ใช้ ดูหัวข้อ 5

#### 3.8.6 การส่งออก

- FR-8.11: ✅ Export โน้ตเพลงเป็นไฟล์ `.xlsx` พร้อมรักษาการจัดรูปแบบ (สี, merge, ขนาดคอลัมน์)

---

## 4. ความต้องการที่ไม่ใช่ฟังก์ชัน (Non-Functional Requirements)

- NFR-1: ระบบต้องรองรับการพิมพ์ภาษาไทยในทุกฟิลด์ รวมถึงฟิลด์โน้ตเพลงที่มีอักขระและสัญลักษณ์พิเศษ
- NFR-2: ระบบ Version History และ Audit Log ต้องไม่สามารถลบหรือแก้ไขย้อนหลังได้ (append-only)
- NFR-3: ระบบต้องรองรับการใช้งานพร้อมกันหลายคนในระบบจองคิวเครื่องดนตรี โดย validation ต้องทำงานแบบ real-time หรือใกล้เคียง เพื่อป้องกันการจองซ้ำซ้อน
- NFR-4: การแสดงวันที่ทั้งหมดใน UI ใช้ปฏิทินไทย (พ.ศ.)
- NFR-5: **Date picker กลาง (shared component)** — ทุกฟีเจอร์ที่ต้องเลือกวันที่ (FR-4.2 สร้างงานแสดง, FR-6.0.1 สร้างโพล, FR-6.1 สร้างตารางซ้อม, FR-6.x แก้ไขตารางซ้อม) ต้องใช้ component ตัวเดียวกันเพื่อความสอดคล้อง UX โดยมีสเปกดังนี้:
  - **Header:** ไอคอนปฏิทิน + label `"Date"` ชิดซ้ายบน; ลิงก์ `"View calendar"` สีฟ้าชิดขวาบน (คลิกเปิด calendar เต็มเพื่อเลือกวันนอกช่วงที่แสดงในแถว)
  - **Body:** แถวปุ่มวันที่ 5 ปุ่มเรียงแนวนอน แต่ละปุ่มแสดงชื่อวันย่อภาษาอังกฤษ (Fri, Sat, Sun, Mon, Tues) บรรทัดบน และเลขวันที่ (17–21) บรรทัดล่าง
  - **State:** ปุ่มที่เลือกมีกรอบสีฟ้า (primary) หนา, ปุ่มปกติกรอบเทาอ่อน; ปุ่มที่ disabled (เกิน min/max ของ context เช่น ก่อนวันนี้ หรือหลังวันแสดง) แสดงแบบจาง กดไม่ได้
  - **Default window:** วันแรกในแถว = วันนี้ (หรือ min date ที่ context กำหนด); เลื่อนแถวได้ผ่าน "View calendar" หรือปุ่ม chevron ซ้าย/ขวา (ถ้ามีที่ว่าง)
  - **Multi-select support:** สำหรับฟีเจอร์ที่เลือกได้หลายวัน (งานแสดง, ตารางซ้อม, โพล) — ปุ่มที่เลือกแล้วทุกอันมีกรอบสีฟ้าพร้อมกัน; single-select สำหรับฟีเจอร์ที่เลือกวันเดียว
  - **ยังคง constraint เดิม:** min = วันนี้ (Bangkok TZ), max ตาม context (วันแสดงล่าสุด/แรก), ข้อความ error/tooltip เป็นภาษาไทย
- NFR-6: **Time range picker กลาง (shared component)** — ทุกฟีเจอร์ที่ต้องเลือก**ช่วงเวลาเริ่ม–สิ้นสุด** (FR-4.2 สร้างงานแสดง, FR-4.2 แก้ไขวันแสดง, FR-6.0.1 สร้างโพล — เฉพาะ slot เวลา, FR-6.1 สร้างตารางซ้อม, FR-6.x แก้ไขตารางซ้อม — เพิ่ม slot) ต้องใช้ component ตัวเดียวกัน โดยมีสเปกดังนี้:
  - **Row layout:** สองแถวสำหรับ "เริ่ม" และ "สิ้นสุด" — แต่ละแถวมีไอคอนนาฬิกา + label ชิดซ้าย, กล่องแสดงเวลาที่เลือก (สีฟ้า/primary) ชิดขวา
  - **Wheel picker:** คลิกกล่องแสดงเวลา → เปิด wheel picker แบบล้อหมุน 2 คอลัมน์ (ชั่วโมง 00–23 + นาที 00–59) inline ใต้แถวนั้น; เปิดได้ทีละคอลัมน์ (เริ่ม หรือ สิ้นสุด)
  - **Wheel behavior:** ใช้ scroll-snap แนวตั้ง, แถวกลางที่เลือกไฮไลต์พื้นหลังสีเทาอ่อน + ตัวอักษรใหญ่/เข้ม, แถวห่างจุดกึ่งกลาง fade ลงเรื่อยๆ (CSS mask gradient); scroll แล้ว snap ไปเลขที่ใกล้ที่สุด
  - **Format:** 24 ชั่วโมง (แสดง HH:MM) เพื่อให้สอดคล้อง preset เดิมของระบบ (10:00, 14:00, 17:00)
  - **Validation:** endTime > startTime; แสดง error inline ถ้า user เลือกเวลาสิ้นสุดก่อนเวลาเริ่ม

---

## 5. ช่องว่างและงานที่ยังคั่งค้าง (Known Gaps & Open Issues)

| หัวข้อ | รายละเอียด | สถานะ |
|---|---|---|
| **Google sign-in (FR-1.1)** | หัวข้อ 3.1 v4.0 เป็น **target design ยังไม่ได้เขียนโค้ด** — ปัจจุบันหน้า `/register` + `/login` ยังเป็น email+password (NextAuth Credentials + bcrypt) สำหรับทุกคนอยู่ | **ยังไม่ implement** |
| **สถานะ auth ในโค้ดปัจจุบัน** | commit `a67337a` (12 ส.ค. 2569) ปิดขั้นตอน email verification เพราะ Resend ส่งเมลไม่เสถียร — `POST /api/auth/register` จึงสร้าง `User` สถานะ `PENDING_APPROVAL` ทันทีโดยไม่ verify อีเมล ส่วนโค้ด Supabase (`/auth/callback`, model `PendingRegistration`, `supabaseUserId`) ยังอยู่ในโปรเจกต์แต่ **ไม่ถูกเรียกใช้** (dead path, tag `v1-email-verification`) | ของเดิมรอลบทิ้งเมื่อย้ายไป Google |
| Migration สมาชิกเดิม → Google | ต้องยืนยันว่าสมาชิกทุกคนมีอีเมลที่ผูก Google account ได้จริง (เช่น อีเมลมหาวิทยาลัย/Gmail) ก่อนตัด password login ทิ้ง มิฉะนั้นจะมีคน login ไม่ได้ | รอตรวจสอบรายชื่ออีเมลสมาชิก |
| บัญชีชั่วคราว (FR-1.5) กับ Google-only | **ตัดสินใจแล้ว (16 ก.ย. 2569): คง Credentials provider ไว้เฉพาะบัญชีชั่วคราว** โดย `authorize()` ต้อง reject ทุก user ที่ `isTemporary !== true` — ข้อนี้คือจุดที่พลาดแล้วเปิดช่องให้สมาชิกปกติ login ด้วย password ได้ ต้องมี test ครอบ | ตัดสินใจแล้ว รอ implement |
| Anti double-booking (FR-4.12) | ยังไม่มี unique constraint / validation กันการจองเครื่องดนตรีซ้ำใน SongAssignment | **ยังไม่ implement** |
| Audit Log wiring (FR-5.5) | Prisma model `AuditLog` มีอยู่แล้ว แต่ยังไม่มี endpoint ใดเรียกเขียน log | **ยังไม่ implement** |
| **สมุดโน้ต: เขียนเองหรือใช้ไลบรารี** | พิจารณาแล้วเมื่อ 16 ก.ย. 2569 — **สรุปว่าเขียนเอง** เพราะ (1) โน้ตไทยมีสระ/วรรณยุกต์ซ้อน + ระบบ rich text ระดับตัวอักษรที่ทำไว้เอง ไลบรารีส่วนใหญ่ไม่รองรับ (2) ต้องย้ายข้อมูล 3,841 เซลล์พร้อม style/merge เข้าโครงสร้างของไลบรารี (3) เกือบทุกตัวผูกมากับระบบสูตรที่เราไม่ใช้ (ดู 3.8.5) · ตัวเลือกที่เคยพิจารณา: Univer, Fortune-sheet, Handsontable, AG Grid · แนวทางคือยืมเฉพาะชิ้นส่วน เช่น `dnd-kit` สำหรับลากสลับแถว/คอลัมน์ | ตัดสินใจแล้ว |
| สมุดโน้ต: ลำดับงานที่เหลือ | ทำไปแล้ว: FR-8.14 (copy/cut), FR-8.15 (undo ครอบทุกอย่าง), FR-8.10 (ลบ/แทรกแถว-คอลัมน์), FR-8.18/8.19/8.22/8.23 (แนวตั้ง, wrap, เส้นขอบ) · เหลือ: FR-8.12 (ย้ายแถว/คอลัมน์), FR-8.13 (drag-drop), FR-8.25 (fill handle), FR-8.20/8.21 (A+/A−, ลบ format), FR-8.24 (gridline) | กำลังดำเนินการ |
| Cmd+Z ไม่ทำงาน (FR-8.15) | ทดสอบพบว่า Ctrl+Z ทำงานแต่ Cmd+Z ไม่ทำงาน ทั้งที่โค้ดเช็ค `metaKey` และ event เข้าถึง handler จริง — ผู้ใช้ส่วนใหญ่อยู่บน Mac | **รอยืนยันด้วยมือบน Mac จริง** |
| Stage search | ไม่มีการค้นหา/กรองผังเวที (ดูได้เฉพาะผ่านการ์ดในหน้ารายการ) | ยังไม่มีแผน |
| เครื่องดนตรีที่เล่นพร้อมกันได้ | ต้องกำหนด list ของเครื่องดนตรีที่ยกเว้นจาก validation การจองซ้ำ (เช่น ระนาดทุ้ม) | รอยืนยันรายชื่อทั้งหมด — จำเป็นก่อน implement FR-4.12 |
| สัญลักษณ์พิเศษในโน้ตเพลง | ต้องรวบรวมชุดสัญลักษณ์ที่วงใช้จริงทั้งหมด เพื่อออกแบบ UI กรอกโน้ตให้ใช้งานง่าย | รอข้อมูลเพิ่มเติม |
| ระบบยืม-ให้ยืมอุปกรณ์ | ถูก revert ออกจาก main (commit 61d1d5b) — รอทบทวนความต้องการก่อนพัฒนาใหม่ | pending decision |
