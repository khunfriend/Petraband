# Database
DATABASE_URL=postgresql://petraband:changeme@localhost:5432/petraband_v2

# Postgres (for docker-compose.prod.yml)
POSTGRES_USER=petraband
POSTGRES_PASSWORD=changeme
POSTGRES_DB=petraband_v2

# NextAuth
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=https://your-domain.com

# App port
PORT=3000
